# Build outputs
build/
*.o
*.exe
*.dll
*.so

# Generated audio from test runs
*.wav

# Local Claude Code settings (machine-specific, not shared)
.claude/settings.local.json
out/

# Scratch space -- either of us may leave anything here
tmp/

# NVDA add-on staging: binaries and the proprietary Textalker images that
# get copied in before packaging. The driver source itself IS tracked.
nvda-addon/synthDrivers/echotalk/*.bin
nvda-addon/synthDrivers/echotalk/THIRD_PARTY_LICENSES.txt
nvda-addon/*.nvda-addon

# Add-on documentation: build_addon.sh generates the HTML from the Markdown
# beside it. The Markdown sources and the GPL text ARE tracked.
nvda-addon/doc/en/*.html

# Textalker images. Proprietary to Street Electronics / APH and NOT
# distributable -- see "Obtaining the Textalker images" in README.md. Users
# extract their own into roms/.
roms/*.bin
# The same images wherever they land -- beside the GUI exe, in a release
# staging folder, in a scratch copy -- matched by the names the tools and the
# driver look for (<stem>.ram.bin / .loader.bin / .obj.bin). The .bin inputs
# under reference_text/ are plain text and do not match these.
*.ram.bin
*.loader.bin
*.obj.bin
[Tt]extalker*.bin
# Everything under roms/ except its README, so a disk image or a renamed
# copy put there by hand is not picked up either.
roms/*
!roms/README.md

# GUI release archives (a release zip is checked for images, but never
# committed either way)
dist/

# Python bytecode from running the tools
__pycache__/
